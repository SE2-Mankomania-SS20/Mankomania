package com.mankomania.game.gamecore.fieldoverlay.fielddata;

/**
 * This enum holds values for storing how to position fields in the overlay vertically.
 */
public enum FieldOverlayRowPosition {
    TOP,
    MIDDLE,
    BOTTOM
}
